# Vite 개발 서버에서는 비워 둡니다. /api 요청은 vite.config.js가 백엔드로 중계합니다.
# 배포 환경에서 프런트와 API 도메인이 다를 때만 아래 주석을 해제합니다.
# VITE_API_BASE_URL=https://example.com/api
# Auth and board APIs share the same base URL; local development uses Vite's /api proxy.
VITE_API_BASE_URL = http://192.168.2.196:8080/api